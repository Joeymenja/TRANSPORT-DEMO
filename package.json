{
  "scripts": {
    "build:prod": "echo 'Building Backend...' && cd backend && npm install && npm run build && echo 'Building Frontend...' && cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bcryptjs": "^3.0.3",
    "pdf-lib": "^1.17.1",
    "pg": "^8.16.3"
  }
}
