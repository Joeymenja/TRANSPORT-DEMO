PORT=3003
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=gvbh_transport

# Security - REQUIRED (Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# Auth Service (if using separate auth service)
AUTH_SERVICE_URL=http://localhost:8081/auth

# CORS Configuration  
CORS_ORIGIN=*
